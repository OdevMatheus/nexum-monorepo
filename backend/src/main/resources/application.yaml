server:
  port: 8080

spring:
  application:
    name: nexum

  flyway:
    enabled: false
    locations: classpath:db/migration
    baseline-on-migrate: true

  # Conexão com o banco
  datasource:
    url: jdbc:postgresql://localhost:5432/nexum_db
    username: nexum_user
    password: nexum_password
    driver-class-name: org.postgresql.Driver

  # Hibernate
  jpa:
    hibernate:
      ddl-auto: validate #Flyway
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  # Conexão com o Redis (cache)
  data:
    redis:
      host: localhost
      port: 6379

  # Conexão com o kafka (eventos)
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: nexum-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer

jwt:
  secret: ${JWT_SECRET}
  access-token-expiration: ${JWT_ACCESS_EXPIRATION:3600000}
  refresh-token-expiration: ${JWT_REFRESH_EXPIRATION:604800000}

resend:
  api-key: ${RESEND_API_KEY}
  from-email: ${RESEND_FROM_EMAIL:noreply@seudominio.com}

app:
  base-url: ${APP_BASE_URL:http://localhost:8080}